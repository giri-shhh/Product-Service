spring:
  application:
    name: product-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configserver
        fast-fail: true
    retry:
      max-attempts: 100
      max-interval: 5000